[indent=4]
uses SDL
uses Emscripten
uses entitas

namespace {{ namespace }}.systems 


    class {{ name }} : Object 
        game : Game
        world: Factory

		construct(game:Game, world:Factory)
			this.game = game
			this.world = world

		def initialize()
            pass

		def execute(delta:double)
            pass
